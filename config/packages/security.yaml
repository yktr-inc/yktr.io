security:
    encoders:
        App\Entity\User:
            algorithm: bcrypt

    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User
                property: username

    firewalls:
      dev:
        pattern: ^/(_(profiler|wdt)|css|images|js)/
        security: false
      main:
        anonymous: true

        form_login:
          check_path: app_front_security_login
          login_path: app_front_security_login
          username_parameter: user_login[username]
          password_parameter: user_login[password]
          default_target_path: app_back_index_dashboard

        logout:
          path: app_logout

    role_hierarchy:
        ROLE_STUDENT: ROLE_USER
        ROLE_ADMINISTRATIVE: ROLE_USER
        ROLE_SUPERADMIN: [ROLE_ADMINISTRATIVE, ROLE_STUDENT]

    access_control:
        - { path: ^/admin, roles: ROLE_SUPERADMIN }
        - { path: ^/dashboard, roles: ROLE_USER }
