{% extends 'Back/layouts/app.html.twig' %}

{% block breadcrumb %}
      <li class="breadcrumb-item"><a href="#">Home</a></li>
      <li class="breadcrumb-item"><a href="{{ path('teacher_course_index') }}">Course</a></li>
      <li class="breadcrumb-item"><a href="{{ path('teacher_course_show', { 'id': course.id }) }}">{{ course.title }}</a></li>
      <li class="breadcrumb-item active" aria-current="page">{{ exam.type|upper}}: {{ exam.name }} </li>
{% endblock %}

{% block stylesheets %}
    {{ encore_entry_link_tags('crud') }}
    {{ encore_entry_link_tags('dashboard') }}
{% endblock %}

{% block body %}
    <div class="col-8 pt-3">
        <div class="card">
            <div class="card-header card-header-{{ colorClass }} dashboard-small-list small-card-header">
                <h4 class="ml-3 card-title">{{ exam.name }}</h4>
            </div>
            <div class="card-body">
                <div class="clear mb-2">
                    <a class="btn btn-default btn-sm mb-2" role="button" href="{{ path('teacher_course_show', { 'id': course.id}) }}">back to list</a>
                </div>
                <table class="table">
                    <tbody>
                    <tr>
                        <th>Name</th>
                        <td>{{ exam.name }}</td>
                    </tr>
                    <tr>
                        <th>Details</th>
                        <td>{{ exam.description }}</td>
                    </tr>
                    <tr>
                        <th>Name</th>
                        <td>{{ exam.name }}</td>
                    </tr>
                    <tr>
                        <th>Date</th>
                        <td>{{ exam.date ? exam.date|date('Y-m-d H:i:s') : '' }}</td>
                    </tr>
                    </tbody>
                </table>

                <a class="float-left mr-2 btn btn-primary btn-sm" href="{{ path('teacher_exam_edit', {'id': exam.id}) }}">edit</a>
                {{ include('Back/exam/teacher/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
    <div class="col-4">
        <div class="card shadow">
            <div class="py-3 ml-3 card-header-{{ colorClass }}">
                <h6 style="color:white!important;" class="m-0 font-weight-bold text-primary">Course Manager</h6>
            </div>
            <div class="card-body mb-2">
                <div class="text-center">
                    <img class="img-fluid px-3 px-sm-4 mb-4" style="width: 20em;margin-top:-20px;margin-bottom:-40px;" src="https://www.arcapers.com/images/digital-marketing/content2.png" alt="">
                </div>
                <p>Remember, if you change the date of the exam, it has to be validated by the Administrative, the students will be notified. You can delete the Exam, but be sure to planify another : don't do this if it's just a Date Error. </p>
                <a target="_blank" rel="nofollow" href="#">Back to Courses Details →</a>
            </div>
        </div>
    </div>

{% endblock %}
