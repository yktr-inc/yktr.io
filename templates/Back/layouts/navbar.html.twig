<nav class="navbar navbar-expand-lg navbar-transparent navbar-absolute fixed-top ">
    <div class="container-fluid row">
        <div id="breadcrum" class="col-3 navbar-wrapper">
            <nav class="align-middle" aria-label="breadcrumb">
                <ol class="breadcrumb" style="margin-bottom:0">
                    {% block breadcrumb %}{% endblock %}
                </ol>
            </nav>
        </div>
        <div class="col-6 align-middle">
            <div class="justify-content-center d-flex">
                <form class="search-form">
                    <input id="search-input" placeholder="Search" class="search-input" />
                    <button type="submit" class="search-button">
                        <i id="search-icon" class="material-icons">search</i>
                    </button>
                </form>
            </div>
        </div>
        <div class="col-3 collapse navbar-collapse justify-content-end">
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="material-icons">notifications</i>
                        <span class="notification">{{ notications.getNotificationsCount }}</span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-right notifications" role="menu" aria-labelledby="dLabel">
                        {% for notif in notications.getNotifications() %}
                        <div class="notifications-wrapper">
                            <a class="content" href="#">
                                <div class="notification-item">
                                    <div class="row">
                                        <div class="col-md-12">
                                            <span class="align-middle item-title">{{ notif.getType|noticationsType }}</span>
                                            <div>{{ notif.getContent }}</div>
                                        </div>
                                        <div class="col-md-12 text-right">
                                            <span class="align-middle badge badge-primary item-time">{{ notif.getCreatedAt|ago }}</span>
                                        </div>
                                    </div>
                                </div>
                            </a>
                        </div>
                        {% endfor %}
                    </ul>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="material-icons">person</i>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="#">My profile</a>
                        <a class="dropdown-item" href="#">Send Message</a>
                        {% if is_granted('ROLE_PREVIOUS_ADMIN') %}
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path(app.request.get('_route'), {'_switch_user': '_exit'}) }}">Exit impersonation</a>
                        {% endif %}
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="/logout">Logout</a>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>
