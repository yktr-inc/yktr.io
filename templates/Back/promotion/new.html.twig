{% extends 'Back/layouts/app.html.twig' %}

{% block breadcrumb %}
      <li class="breadcrumb-item"><a href="{{ path('dashboard')}}">Home</a></li>
      <li class="breadcrumb-item"><a href="{{ path('promotion_index')}}">Promotions</a></li>
      <li class="breadcrumb-item active">New</li>
{% endblock %}


{% block body %}
<div class="card m-auto w-75">
      <div class="card-header card-header-primary">
        <h4 class="card-title">Add a new promotion</h4>
    </div>
    <div class="card-body">
    {{ include('Back/promotion/_form.html.twig') }}
    <a href="{{ path('promotion_index') }}">back to list</a>
    </div>
</div>
{% endblock %}
