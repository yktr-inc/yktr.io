{% extends 'Back/layouts/app.html.twig' %}

{% block breadcrumb %}
<li class="breadcrumb-item"><a href="{{ path('dashboard')}}">Home</a></li>
<li class="breadcrumb-item"><a href="{{ path('course_index')}}">Course</a></li>
<li class="breadcrumb-item active">Edit {{ course.title }}</li>
{% endblock %}

{% block stylesheets %}
    {{ encore_entry_link_tags('crud') }}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('js/crud') }}
{% endblock %}

{% block body %}
<div class="card m-auto w-90">
  <div class="card-header card-header-primary row">
    <div class="col-md-10">
      <h4 class="card-title">Edit course</h4>
    </div>
    <div class="col-md-2">
      {{ include('Back/course/_delete_form.html.twig') }}
    </div>
  </div>
  <div class="card-body">
    <div class="clear mb-2">
      <a class="btn btn-default btn-sm" role="button" href="{{ path('course_index') }}">back to list</a>
    </div>
    {{ include('Back/course/_form.html.twig', {'button_label': 'Update'}) }}
  </div>
</div>
{% endblock %}
